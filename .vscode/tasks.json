{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Tests (Release)",
            "type": "shell",
            "command": "if (!(Test-Path build)) { New-Item -ItemType Directory build | Out-Null }; cd build; cmake ..; cmake --build . --config Release; .\\Release\\pufferfish_tests.exe",
            "options": {
                "cwd": "${workspaceFolder}/pufferfish"
            },
            "group": {
                "kind": "test",
                "isDefault": true
            },
            "problemMatcher": []
        },
        {
            "label": "Build Tests (Release)",
            "type": "shell",
            "command": "if (!(Test-Path build)) { New-Item -ItemType Directory build | Out-Null }; cd build; cmake ..; cmake --build . --config Release",
            "options": {
                "cwd": "${workspaceFolder}/pufferfish"
            },
            "group": "build",
            "problemMatcher": []
        }
    ]
}